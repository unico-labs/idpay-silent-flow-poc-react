# Copy to .env.local (already gitignored) and fill in real values.
# Never commit .env.local.

VITE_SDK_KEY=
VITE_COMPANY_ID=

# Optional: test against a local mirrord session instead of the real UAT
# deployment. Leave both empty for the default behavior (proxy /idpay to
# UAT, no mirrord header).
#
# VITE_API_TARGET=http://localhost:8888
# VITE_MIRRORD_USER=<your-username>
